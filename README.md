# protect-probe-20260923

A throwaway repository. It measured, on 2026-09-23, whether a user-owned
repository's rulesets can (1) require a status check on pull requests into
`main` while GitHub Actions' own `GITHUB_TOKEN` pushes still land, and (2)
restrict updates to a branch with GitHub Actions on the bypass list.
Archived when done; nothing here is used by anything.
